---
name: full-skill
description: Demonstrate a registry-ready skill package with references, scripts, assets, evaluation, capabilities, and language binding metadata.
license: MIT
metadata:
  version: "0.1.0"
---

# Full Skill

Use this skill when a user asks for a complete package example.

## Workflow

1. Read `workflow.md` when the user asks how a complete package is organized.
2. Run `validate.sh` only when the user asks to validate the example package.
3. Use `report.md` as a report template when producing a package review.

## Output

Produce a concise package review with sections for structure, metadata, commands, security, and evaluation.
